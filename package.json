{
  "name": "root",
  "private": true,
  "scripts": {
    "dev:web": "yarn --cwd packages/mono.web dev",
    "dev": "yarn dev:web",
    "build": "tsc -b packages/**/tsconfig.build.json && lerna run build",
    "start": "yarn --cwd packages/mono.web start"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "dependencies": {
    "module-alias": "^2.2.2"
  },
  "_moduleAliases": {
    "@mono/lib": "packages/mono.lib/dist"
  }
}